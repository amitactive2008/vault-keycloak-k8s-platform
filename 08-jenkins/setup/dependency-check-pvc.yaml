apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dependency-check-data
  namespace: jenkins
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi

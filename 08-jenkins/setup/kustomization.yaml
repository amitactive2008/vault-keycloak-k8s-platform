apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jenkins

resources:
  - namespace.yaml
  - rbac.yaml
  - buildkitd.yaml
  - buildkit-certs-stub.yaml
  - dependency-check-pvc.yaml

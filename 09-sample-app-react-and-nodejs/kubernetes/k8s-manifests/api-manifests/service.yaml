apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: dev
spec:
  selector:
    app.kubernetes.io/name: api
    app.kubernetes.io/instance: api-dev
  ports:
  - port: 5000
    targetPort: 5000
  type: ClusterIP



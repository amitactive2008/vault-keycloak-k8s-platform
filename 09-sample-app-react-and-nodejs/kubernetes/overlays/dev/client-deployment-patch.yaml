apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
  namespace: dev
  labels:
    app.kubernetes.io/instance: client-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/instance: client-dev
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: client-dev


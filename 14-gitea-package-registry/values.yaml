image:
  repository: docker.gitea.com/gitea
  tag: 1.26.4-rootless

persistence:
  size: 20Gi
  storageClassName: standard

gateway:
  name: native-gateway
  namespace: default
  hostname: gitea.kind.local

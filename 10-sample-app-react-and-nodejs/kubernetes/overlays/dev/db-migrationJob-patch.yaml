apiVersion: batch/v1
kind: Job
metadata:
  name: database-migration
  namespace: dev
spec:
  template:
    spec:
      containers:
      - name: migration
        image: amitactive2008/api:latest
        resources:
          requests: 
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
